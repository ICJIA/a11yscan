[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter a11yscan-web generate"
  publish = "packages/web/.output/public"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
